# 上游合并与本地提交迁移规则

本文件适用于本仓库中所有涉及同步、合并、变基、冲突处理和上游升级的工作。

## 核心原则

- 上游仓库是代码结构、接口形态和默认行为的基准。
- 合并上游时，以上游版本覆盖本地项目；发生同一位置冲突时，优先保留上游实现。
- “保留上游”只决定冲突处的实现方式，不代表可以删除本地已有提交、功能、修复或兼容行为。
- 本地提交必须以上游最新代码为基础重新适配、移植或重放；禁止直接覆盖后假定功能仍然存在。
- 已有本地功能应优先通过兼容层、转换器、路由适配或迁移代码接入上游结构。

## 提交列表
本项目重点提交列表，完成合并后以下提交的实现不得丢失。
- `6ab109d feat: support OpenAI image generation and edits`

## 标准流程

1. 合并前先获取上游最新引用，并确认当前分支、目标上游分支和工作区状态。
2. 在任何覆盖或重置前创建可回退的备份分支或标签。
3. 以共同祖先为基准列出本地独有提交：
   `git log --oneline <upstream>/<branch>..HEAD`
4. 先将代码基线更新到上游；冲突解决以保留上游结构和行为为默认选择。
5. 按提交逐项检查本地独有提交，将其意图迁移到上游代码中。优先使用 `cherry-pick`、等价补丁或手工移植；不要用 `merge -X theirs`、批量覆盖文件等方式跳过迁移。
6. 每个本地提交都要得到以下结果之一：
   - 在上游结构中保留等价功能；
   - 以兼容实现替代，并保留原有外部行为；
   - 被上游等价实现明确取代，并在提交说明或工作记录中说明对应关系。
7. 对路由、API、数据模型、授权、转换器和错误处理等边界做回归检查；必要时补充针对迁移功能的最小测试。
8. 完成后确认工作区干净、提交历史可追溯，并明确推送目标远程；未经明确授权不得强制覆盖他人远程分支。

## 冲突处理

- 同一代码位置冲突：保留上游代码形态，再把本地提交的必要行为移植到合适的新位置。
- 上游删除了本地文件或接口：先判断本地提交依赖的公开行为，再用上游架构提供兼容入口；不能仅因文件被删除就放弃功能。
- 上游重命名或重构模块：迁移行为和测试，不机械恢复旧文件布局。
- 数据库或配置变更：以上游 schema 为准，通过迁移、默认值或兼容读取保护已有数据。
- 无法安全判断本地提交是否已被上游覆盖时，保留本地提交意图并标记待确认，不得静默丢弃。

## 验证要求

- 至少运行受影响模块的测试和静态检查；跨模块改动时运行完整测试。
- 检查 `git diff --check`，确认没有无关生成物、临时文件或凭据。
- 汇报时同时说明：上游基线、本地提交迁移结果、冲突取舍、验证命令和推送目标。
