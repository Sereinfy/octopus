# 上游同步、架构适配与提交说明规则

本文件用于指导 AI 助手在修改、增加或移植功能，以及合并上游提交、同步分支或适配上游架构变化时进行工作。

## 一、基本原则

1. 修改或增加功能时，必须以**当前项目的最新架构和实际代码状态**为基准；合并或同步上游时，以上游待合并提交的架构和实际代码为适配基准，将本项目仍需保留的修改移植到上游状态。不得因为移植功能而破坏最终采用的架构。
2. 开始修改前，应先了解当前项目的模块边界、接口约定、配置方式、数据流和关键流程，并确认工作区及本项目已有提交，避免基于过时架构实施改动。
3. 当新旧架构、接口或配置不兼容时，应优先通过适配层、兼容处理或渐进式迁移保持现有行为；确需调整时，应明确记录行为差异及影响范围。
4. 应区分上游改动与本项目已有改动的目的和影响；同步上游时应以上游当前架构和行为为基准适配本项目改动，不得直接用任一方覆盖另一方而丢失仍需保留的功能。

## 二、合并上游提交

1. 合并上游提交发生冲突时，应以上游提交的当前代码和架构为基准，将本项目已有修改和新增功能适配移植到上游状态；不得直接用上游版本覆盖并丢失本项目仍需保留的功能。
2. 如果上游改动会删除、替换或改变本项目已有功能，应先判断该功能能否适配到上游状态；能够适配的，按上游实现调整本项目。这里的“无法移植”仅指较大型的架构或功能变化，例如功能合并、拆分、整体替换或删除后已无法建立合理对应关系，不包括可通过常规冲突解决、接口适配或小范围重构处理的问题；确实无法移植时，才不跟随造成冲突的上游改动，并保留本项目对应功能，同时记录原因和影响。
3. 如果本项目已经明确删除或不再保留某项功能，即使上游仍保留或继续修改该功能，也应以**本项目当前状态为准**，不恢复或回移该功能；仅移植不依赖该功能的上游改动。
4. 如果冲突无法在不改变行为的前提下解决，应保留本项目的功能实现，并明确记录冲突原因、处理方式、行为变化以及需要人工确认的内容。
5. 合并完成后，应检查关键功能、接口、配置和数据库行为，确认本项目原有功能没有回退；同时运行项目中适用的测试、检查和构建流程。

## 三、上游架构发生变化

1. 如果上游架构发生变化并导致文件路径、模块结构或接口改动较大，应将本项目已有的修改和新增功能移植到**上游采用的最新架构**中，而不是放弃这些修改、强行恢复旧架构，或简单复制旧文件。
2. 移植前应整理本项目的功能改动、配置变更、接口调整、数据库迁移和测试，并建立它们与新架构对应位置的关系。
3. 移植过程中应以上游的架构边界、API 和配置格式为准，将本项目既有行为通过适配层或兼容处理保留下来；对于必须调整的部分，应说明调整原因、行为差异和迁移影响。
4. 移植完成后，应重点验证本项目新增功能、原有关键流程以及上游架构本身的核心流程，避免只验证代码能够编译。
5. 对于无法自动验证的内容，应在变更说明中列出需要人工检查的步骤、已知风险和可能的回滚方式。

## 四、处理顺序

1. 开始前先查看当前工作区、项目最新架构和本项目已有提交，识别并避让未提交的本地修改。
2. 先完成冲突分析或架构对应关系整理，再进行实际移植和修改。
3. 修改完成后检查差异，确认没有遗漏本项目的功能、文件、配置、数据库变更或测试。
4. 按影响范围运行适用的测试、静态检查、构建和必要的手工验证。
5. 在合并或移植说明中记录：涉及的冲突或架构变化、保留和调整的本项目功能、兼容性处理、验证结果，以及尚需人工确认的问题。

## 五、Git 提交与同步说明

1. 代码完成后，应提供简明的变更简报；在进行 Git 提交、分支同步、上游合并或提交相关变更说明时，应一并携带该简报的核心内容。
2. 提交或同步说明不要求与代码完成时的简报完全一致，但必须保留重点，至少说明：做了什么、为什么做、兼容性或架构影响、验证情况，以及尚需人工确认的问题（如有）。
3. 标题和详情应尽量使用中文；如果使用英文术语或措辞更直观、准确，可保留必要的英文，例如 API、Git、refactor、compatibility fix feat 等github提交时常用的。
4. 说明应简洁、准确、可追溯，避免只写“同步上游”“修复问题”等无法反映实际变更的笼统描述。
5. 推荐采用以下结构：

   ```text
   标题：<变更类型>: <核心变更>

   详情：
   - 变更内容：<完成了什么>
   - 架构与兼容性：<对当前架构、接口或配置的影响；无影响可省略>
   - 验证结果：<执行的测试、检查、构建或手工验证>
   - 待确认事项：<需要人工确认的问题；无则省略>
   ```
