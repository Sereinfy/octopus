# 上游同步与架构迁移规则

本文件用于指导 AI 助手在合并上游提交、同步分支或适配上游架构变化时进行修改。

## 一、合并上游提交

1. 合并上游提交发生冲突时，应当以**不影响本项目原有提交内容、已有修改和新增功能**为原则处理冲突。
2. 对于冲突文件，应先区分上游改动与本项目改动的目的和影响，再将双方内容以兼容方式合并；不能直接用上游版本覆盖本项目已有实现。
3. 如果上游改动会删除、替换或改变本项目已有功能，应优先保留本项目功能，并在此基础上吸收上游改动。
4. 如果冲突无法在不改变行为的前提下解决，应保留本项目的功能实现，明确记录冲突原因、处理方式以及需要人工确认的内容。
5. 合并完成后，应检查关键功能、接口、配置和数据库行为，确认本项目原有功能没有回退；同时运行项目中适用的测试、检查和构建流程。

## 二、上游架构发生变化

1. 如果上游架构发生变化并导致文件路径、模块结构或接口改动较大，应当把本项目提交中已有的修改和新增功能移植到新架构中，而不是放弃这些修改或直接恢复旧架构。
2. 移植前应整理本项目的功能改动、配置变更、接口调整、数据库迁移和测试，并建立它们与新架构对应位置的关系。
3. 移植过程中应尽量保持本项目原有的 API、配置格式和行为；如果新旧架构不兼容，应增加必要的兼容处理，并说明行为差异。
4. 移植完成后，应重点验证本项目新增功能、原有关键流程以及上游架构本身的核心流程，避免只验证代码能够编译。
5. 对于无法自动验证的内容，应在变更说明中列出需要人工检查的步骤、已知风险和可能的回滚方式。

## 三、处理顺序

1. 开始前先查看当前工作区和本项目已有提交，避免覆盖未提交的本地修改。
2. 先完成冲突分析或架构对应关系整理，再进行实际移植和修改。
3. 修改完成后检查差异，确认没有遗漏本项目的功能、文件或配置。
4. 在合并或移植说明中记录：涉及的冲突或架构变化、保留和调整的本项目功能、验证结果，以及尚需人工确认的问题。
